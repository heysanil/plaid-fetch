/* tslint:disable */
/* eslint-disable */
/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.503.5
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SignalDevice } from './SignalDevice';
import {
    SignalDeviceFromJSON,
    SignalDeviceFromJSONTyped,
    SignalDeviceToJSON,
} from './SignalDevice';
import type { SignalUser } from './SignalUser';
import {
    SignalUserFromJSON,
    SignalUserFromJSONTyped,
    SignalUserToJSON,
} from './SignalUser';

/**
 * SignalEvaluateRequest defines the request schema for `/signal/evaluate`
 * @export
 * @interface SignalEvaluateRequest
 */
export interface SignalEvaluateRequest {
    /**
     * Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    client_id?: string;
    /**
     * Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    secret?: string;
    /**
     * The access token associated with the Item data is being requested for.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    access_token: string;
    /**
     * The Plaid `account_id` of the account that is the funding source for the proposed transaction. The `account_id` is returned in the `/accounts/get` endpoint as well as the [`onSuccess`](/docs/link/ios/#link-ios-onsuccess-linkSuccess-metadata-accounts-id) callback metadata.
     * 
     * This will return an [`INVALID_ACCOUNT_ID`](/docs/errors/invalid-input/#invalid_account_id) error if the account has been removed at the bank or if the `account_id` is no longer valid.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    account_id: string;
    /**
     * The unique ID that you would like to use to refer to this transaction. For your convenience mapping your internal data, you could use your internal ID/identifier for this transaction. The max length for this field is 36 characters.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    client_transaction_id: string;
    /**
     * The transaction amount, in USD (e.g. `102.05`)
     * @type {number}
     * @memberof SignalEvaluateRequest
     */
    amount: number;
    /**
     * `true` if the end user is present while initiating the ACH transfer and the endpoint is being called; `false` otherwise (for example, when the ACH transfer is scheduled and the end user is not present, or you call this endpoint after the ACH transfer but before submitting the Nacha file for ACH processing).
     * @type {boolean}
     * @memberof SignalEvaluateRequest
     */
    user_present?: boolean;
    /**
     * A unique ID that identifies the end user in your system. This ID is used to correlate requests by a user with multiple Items. Personally identifiable information, such as an email address or phone number, should not be used in the `client_user_id`.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    client_user_id?: string;
    /**
     * `true` if the ACH transaction is a recurring transaction; `false` otherwise 
     * @type {boolean}
     * @memberof SignalEvaluateRequest
     */
    is_recurring?: boolean;
    /**
     * The default ACH or non-ACH payment method to complete the transaction.
     * `SAME_DAY_ACH`: Same Day ACH by NACHA. The debit transaction is processed and settled on the same day
     * `NEXT_DAY_ACH`: Next Day ACH settlement for debit transactions, offered by some payment processors
     * `STANDARD_ACH`: standard ACH by NACHA
     * `REAL_TIME_PAYMENTS`: real-time payments such as RTP and FedNow
     * `DEBIT_CARD`: if the default payment is over debit card networks
     * `MULTIPLE_PAYMENT_METHODS`: if there is no default debit rail or there are multiple payment methods
     * Possible values:  `SAME_DAY_ACH`, `NEXT_DAY_ACH`, `STANDARD_ACH`, `REAL_TIME_PAYMENTS`, `DEBIT_CARD`, `MULTIPLE_PAYMENT_METHODS`
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    default_payment_method?: string;
    /**
     * 
     * @type {SignalUser}
     * @memberof SignalEvaluateRequest
     */
    user?: SignalUser;
    /**
     * 
     * @type {SignalDevice}
     * @memberof SignalEvaluateRequest
     */
    device?: SignalDevice;
    /**
     * The name of the risk profile to use for this transaction. A risk profile can be configured using via the Plaid Dashboard. If not provided, no risk profile will be used. This feature is currently in closed beta; to request access, contact your account manager.
     * @type {string}
     * @memberof SignalEvaluateRequest
     */
    risk_profile_key?: string;
}

/**
 * Check if a given object implements the SignalEvaluateRequest interface.
 */
export function instanceOfSignalEvaluateRequest(value: object): boolean {
    if (!('access_token' in value)) return false;
    if (!('account_id' in value)) return false;
    if (!('client_transaction_id' in value)) return false;
    if (!('amount' in value)) return false;
    return true;
}

export function SignalEvaluateRequestFromJSON(json: any): SignalEvaluateRequest {
    return SignalEvaluateRequestFromJSONTyped(json, false);
}

export function SignalEvaluateRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): SignalEvaluateRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'client_id': json['client_id'] == null ? undefined : json['client_id'],
        'secret': json['secret'] == null ? undefined : json['secret'],
        'access_token': json['access_token'],
        'account_id': json['account_id'],
        'client_transaction_id': json['client_transaction_id'],
        'amount': json['amount'],
        'user_present': json['user_present'] == null ? undefined : json['user_present'],
        'client_user_id': json['client_user_id'] == null ? undefined : json['client_user_id'],
        'is_recurring': json['is_recurring'] == null ? undefined : json['is_recurring'],
        'default_payment_method': json['default_payment_method'] == null ? undefined : json['default_payment_method'],
        'user': json['user'] == null ? undefined : SignalUserFromJSON(json['user']),
        'device': json['device'] == null ? undefined : SignalDeviceFromJSON(json['device']),
        'risk_profile_key': json['risk_profile_key'] == null ? undefined : json['risk_profile_key'],
    };
}

export function SignalEvaluateRequestToJSON(value?: SignalEvaluateRequest | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'client_id': value['client_id'],
        'secret': value['secret'],
        'access_token': value['access_token'],
        'account_id': value['account_id'],
        'client_transaction_id': value['client_transaction_id'],
        'amount': value['amount'],
        'user_present': value['user_present'],
        'client_user_id': value['client_user_id'],
        'is_recurring': value['is_recurring'],
        'default_payment_method': value['default_payment_method'],
        'user': SignalUserToJSON(value['user']),
        'device': SignalDeviceToJSON(value['device']),
        'risk_profile_key': value['risk_profile_key'],
    };
}

