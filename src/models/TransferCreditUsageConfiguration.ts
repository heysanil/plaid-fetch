/* tslint:disable */
/* eslint-disable */
/**
 * The Plaid API
 * The Plaid REST API. Please see https://plaid.com/docs/api for more details.
 *
 * The version of the OpenAPI document: 2020-09-14_1.503.5
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { CreditACHClass } from './CreditACHClass';
import {
    CreditACHClassFromJSON,
    CreditACHClassFromJSONTyped,
    CreditACHClassToJSON,
} from './CreditACHClass';
import type { OriginatorExpectedTransferFrequency } from './OriginatorExpectedTransferFrequency';
import {
    OriginatorExpectedTransferFrequencyFromJSON,
    OriginatorExpectedTransferFrequencyFromJSONTyped,
    OriginatorExpectedTransferFrequencyToJSON,
} from './OriginatorExpectedTransferFrequency';

/**
 * Specifies the originator's expected usage of credits. For all dollar amounts, use a decimal string with two digits of precision e.g. "10.00". This field is required if the originator is expected to process credit transfers.
 * @export
 * @interface TransferCreditUsageConfiguration
 */
export interface TransferCreditUsageConfiguration {
    /**
     * 
     * @type {OriginatorExpectedTransferFrequency}
     * @memberof TransferCreditUsageConfiguration
     */
    expected_frequency: OriginatorExpectedTransferFrequency;
    /**
     * The originator’s expected highest amount for a single credit transfer.
     * @type {string}
     * @memberof TransferCreditUsageConfiguration
     */
    expected_highest_amount: string;
    /**
     * The originator’s expected average amount per credit.
     * @type {string}
     * @memberof TransferCreditUsageConfiguration
     */
    expected_average_amount: string;
    /**
     * The originator’s monthly expected ACH credit processing amount for the next 6-12 months.
     * @type {string}
     * @memberof TransferCreditUsageConfiguration
     */
    expected_monthly_amount: string;
    /**
     * Specifies the expected use cases for the originator’s credit transfers. This should be a list that contains one or more of the following codes:
     * 
     * `"ccd"` - Corporate Credit or Debit - fund transfer between two corporate bank accounts
     * 
     * `"ppd"` - Prearranged Payment or Deposit - the transfer is part of a pre-existing relationship with a consumer, e.g. bill payment
     * 
     * `"web"` - A credit Entry initiated by or on behalf of a holder of a Consumer Account that is intended for a Consumer Account of a Receiver
     * @type {Array<CreditACHClass>}
     * @memberof TransferCreditUsageConfiguration
     */
    sec_codes: Array<CreditACHClass>;
}

/**
 * Check if a given object implements the TransferCreditUsageConfiguration interface.
 */
export function instanceOfTransferCreditUsageConfiguration(value: object): boolean {
    if (!('expected_frequency' in value)) return false;
    if (!('expected_highest_amount' in value)) return false;
    if (!('expected_average_amount' in value)) return false;
    if (!('expected_monthly_amount' in value)) return false;
    if (!('sec_codes' in value)) return false;
    return true;
}

export function TransferCreditUsageConfigurationFromJSON(json: any): TransferCreditUsageConfiguration {
    return TransferCreditUsageConfigurationFromJSONTyped(json, false);
}

export function TransferCreditUsageConfigurationFromJSONTyped(json: any, ignoreDiscriminator: boolean): TransferCreditUsageConfiguration {
    if (json == null) {
        return json;
    }
    return {
        
        'expected_frequency': OriginatorExpectedTransferFrequencyFromJSON(json['expected_frequency']),
        'expected_highest_amount': json['expected_highest_amount'],
        'expected_average_amount': json['expected_average_amount'],
        'expected_monthly_amount': json['expected_monthly_amount'],
        'sec_codes': ((json['sec_codes'] as Array<any>).map(CreditACHClassFromJSON)),
    };
}

export function TransferCreditUsageConfigurationToJSON(value?: TransferCreditUsageConfiguration | null): any {
    if (value == null) {
        return value;
    }
    return {
        
        'expected_frequency': OriginatorExpectedTransferFrequencyToJSON(value['expected_frequency']),
        'expected_highest_amount': value['expected_highest_amount'],
        'expected_average_amount': value['expected_average_amount'],
        'expected_monthly_amount': value['expected_monthly_amount'],
        'sec_codes': ((value['sec_codes'] as Array<any>).map(CreditACHClassToJSON)),
    };
}

